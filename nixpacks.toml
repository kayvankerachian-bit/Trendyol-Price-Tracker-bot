providers = ["python"]

[phases.setup]
nixPkgs = ["python311","nodejs"]

[phases.install]
cmds = ["pip install -r requirements.txt","python -m playwright install chromium"]

[start]
cmd = "python main.py"
